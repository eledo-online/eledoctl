"""Authentication commands will be implemented later.

Token storage intentionally belongs to eledoctl, not pyeledo. The initial CLI accepts
explicit --token parameters and passes them to pyeledo directly.
"""
